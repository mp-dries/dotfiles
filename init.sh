#!/usr/bin/env bash

# TODO: dynamically load paths from path file and add them to the system path variable

# Load the environment variables
# TODO: do the same for general use variables in variables
